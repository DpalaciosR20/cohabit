# PostgreSQL connection string. Run `npx prisma dev` for a local instance.
DATABASE_URL=""

# Auth.js secret, used to sign/encrypt session JWTs.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=""
